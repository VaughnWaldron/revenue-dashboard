# Admin password for the /admin sign-in screen.
# Choose a strong, unique value — this gates write access to all reports.
ADMIN_PASSWORD=

# Secret used to sign the HttpOnly admin session cookie.
# Generate one with: openssl rand -base64 32
SESSION_SECRET=
